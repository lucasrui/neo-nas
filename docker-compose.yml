version: "3.8"

services:
  backup:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./config:/config
      - ./sourcetest:/source
      - ./targettest:/target
    restart: unless-stopped
